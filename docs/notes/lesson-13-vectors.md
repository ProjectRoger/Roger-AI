1. What is a vector?

A Vector is a ordered collection of numbers.

2. Why do neural networks use vectors?

Neural networks can have any number of inputs in order to properly store this neural networks uses vectors.

3. What is a dot product?

Dot product is the Weighted Sum of the Multiplication of the individual matching numbers.

4. Why must vectors have compatible dimensions?

The vectors must have compatible dimensions so that we can easily do any type of mathematical operations mostly Dot Product.

5. What is a bias?

Bias is a starting adjustment in order to shift the neuron's decision boundary otherwise it will have fixed starting point.

6. What is the equation y = f(w · x + b) describing?

The equation is describing the neural network equation where the weights and inputs are multiplied and then added a bias term and passed through an activation funtion.

7. Why can an image be represented as a vector?

Image can be represented as an vector as an image is a combination of numbers only. For eg. 28 * 28 pixels image is 784 numbers vector.

8. What is an embedding?

The embeddings are generated from the text in order to understand the semantic meaning of the text.

9. Why are dot products important for transformers?

Dot Products are very important as small as neuron also uses the vectors for the calculations and Tranformers is a part of neural study which makes it equally important for it too.